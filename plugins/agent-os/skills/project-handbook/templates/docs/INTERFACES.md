# Interfaces

Record cross-module, external, and provider-facing contracts here, or link to the machine-readable source of truth.

Give each interface a stable interface identifier so requirements and evidence can cite it without copying the contract. For each interface, document inputs and outputs, validation, errors, state changes, idempotency, retries, timeouts, compatibility, examples, and maintainer. Link to the owning machine-readable schema, type, or specification instead of duplicating it.

## Interfaces

### INT-1: <interface name>

- Owner: <module or maintainer>
- Contract: <link to its schema, type definition, or specification>
- Semantics: <inputs, outputs, validation, errors, state changes, and compatibility>
