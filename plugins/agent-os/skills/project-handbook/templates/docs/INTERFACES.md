# Interfaces

Record cross-module, external, and provider-facing contracts here, or link to the machine-readable source of truth.

For each interface, document inputs and outputs, validation, errors, state changes, idempotency, retries, timeouts, compatibility, examples, and maintainer.

## Interfaces

- `<interface>`: <link to its schema, type definition, or specification>
