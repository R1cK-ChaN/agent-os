# Requirements

Use stable `REQ-x.y` identifiers so requirements can be cited without repeating their meaning. Keep each requirement atomic and testable.

## Requirements

- REQ-1.1: <describe one required behavior or constraint>
